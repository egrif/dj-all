#!/usr/bin/env ruby

lib_path = File.join(File.dirname(__FILE__), "..", "lib")
absolute_lib_path = File.expand_path(lib_path)
$LOAD_PATH.unshift(absolute_lib_path) unless $LOAD_PATH.include?(absolute_lib_path)

require 'irb'

# Dir[File.join(".", 'lib/juxtaconfig/**', '*.rb')].each do |file|
#  puts "#{file}"
#  require file
# end

require 'juxtaconfig'

IRB.start